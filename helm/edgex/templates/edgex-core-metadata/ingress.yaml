apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/app.root: /{{ .Values.edgex.core.metadata.name }}
  name: {{ .Values.edgex.core.metadata.name }}
  labels:
    {{- include "edgex.labels" (dict "ctx" . "AppName" $.Values.edgex.core.metadata.name) | indent 4 }}
spec:
  rules:
  - host: metadata.edgex.com
    http:
      paths:
      - path: /
        backend:
          serviceName: {{ .Values.edgex.core.metadata.name }}
          servicePort: {{ .Values.edgex.core.metadata.port }}